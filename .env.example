# Local development only. Production credentials come from the Amplify
# compute role (themis-lex-amplify-compute-role). See DEPLOY.md.

# .env.example — commit this, not .env.local

# AWS Bedrock
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
BEDROCK_MODEL_ID=us.anthropic.claude-sonnet-4-6

# App
NEXT_PUBLIC_APP_VERSION=0.4
